# ── Phase 1: Data acquisition & exploration ───────────────────────────────────
earthengine-api>=0.1.370    # Google Earth Engine Python API
numpy>=1.24                 # Array operations on raster bands
matplotlib>=3.7             # Plotting / visualization in notebooks
folium>=0.14                # Interactive maps (GEE tile overlay)
jupyter>=1.0                # Notebook environment
ipykernel>=6.0              # Jupyter kernel

# ── Phase 2: Local processing & classification ────────────────────────────────
rasterio>=1.3               # Read/write GeoTIFF rasters
GDAL>=3.6                   # Geospatial transforms (installed separately - see README)
scipy>=1.10                 # Spatial statistics, signal processing
shapely>=2.0                # Geometric operations (AOI clipping)
geopandas>=0.14             # Vector data (shapefiles, GeoJSON)
pyproj>=3.6                 # CRS transformations
scikit-learn>=1.3           # Random Forest classifier
xgboost>=2.0                # Gradient boosting (alternative classifier)
joblib>=1.3                 # Model serialization
Pillow>=10.0                # Image utilities

# ── Phase 3: Change analysis & visualisation ──────────────────────────────────
pandas>=2.0                 # DataFrames for area time series and transition matrices
plotly>=5.18                # Interactive Sankey diagrams and charts

# ── Phase 4: Backend API ──────────────────────────────────────────────────────
fastapi>=0.100              # REST API framework
uvicorn>=0.23               # ASGI server
sqlalchemy>=2.0             # ORM
geoalchemy2>=0.14           # Spatial ORM extension (PostGIS)
alembic>=1.12               # Database migrations
psycopg2-binary>=2.9        # PostgreSQL driver
pydantic>=2.0               # Data validation / schemas
celery>=5.3                 # Task queue for long-running jobs
redis>=5.0                  # Celery broker + result backend
httpx>=0.25                 # Async HTTP client (API tests)

# ── Phase 5: Map tile serving ─────────────────────────────────────────────────
rio-tiler>=6.0              # On-the-fly raster tiling

# ── Dev / testing ─────────────────────────────────────────────────────────────
pytest>=7.4
pytest-asyncio>=0.21
ruff>=0.1                   # Linter / formatter
mypy>=1.5                   # Type checker
